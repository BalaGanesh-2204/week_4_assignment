[project]
name = "bala-support-agent"
version = "0.1.0"
description = "E-commerce support agent with RAG, hybrid search, guardrails, memory, logging and escalation to a human (Balaganesh)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.45.0",
    "groq>=1.0.0",
    "google-genai>=1.0.0",
    "pinecone>=6.0.0",
    "python-dotenv>=1.0.0",
    "rank-bm25>=0.2.2",
    "pydantic>=2.7.0",
]
